Sidebar navigation row. Active item gets brand tint + brand text. Group under mono uppercase section labels (PRACOWNIK, PLANOWANIE, DYREKTOR, ADMINISTRACJA).

```jsx
<NavItem icon={<GridIcon/>} label="Pulpit" active onClick={go} />
```
KPI / metric tile. Figure is Archivo 800 with tabular numerals. Use `accent` to flag attention values.

```jsx
<StatCard label="Zalega z urlopem" value="14" unit="osób" sub="powyżej progu zaległości" accent="var(--amber)" />
```
Thin progress/utilisation bar. Supports stacked segments for "wykorzystano + zaległe".

```jsx
<ProgressBar segments={[{pct:42,color:'var(--brand)'},{pct:15,color:'var(--absence-border)'}]} />
<ProgressBar value={83} />
```
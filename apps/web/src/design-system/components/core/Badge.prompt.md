Small status/label pill. Use `mono` for codes & periods ("Sprint 13", "Rok kalendarzowy 2026"), `tone` for status. Never use colour to encode absence type.

```jsx
<Badge tone="brand" mono>Sprint 13</Badge>
<Badge tone="blue" dot>Aktywny</Badge>
```
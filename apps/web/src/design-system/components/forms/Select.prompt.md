Dropdown select. Use for absence type, sprint picker, employment-form filter.

```jsx
<Select label="Typ nieobecności" value={typ} options={['Urlop wypoczynkowy','Urlop na żądanie']} onChange={setTyp} />
```